// ---------------------------------------------------------------------------
// Fill these in with your Supabase project's values, then commit and push.
//
// Find them in the Supabase dashboard under Project Settings → API:
//   SUPABASE_URL      the "Project URL"
//   SUPABASE_ANON_KEY the "anon" / "public" key
//
// Both are safe to commit. The publishable (anon) key is designed to ship to
// browsers — it grants nothing on its own, because the row level security
// policies in supabase/schema.sql are what decide who can read and write.
//
// Never put a "secret" or "service_role" key here. Those bypass every policy,
// and anything in this file is served to the public as part of the page.
// ---------------------------------------------------------------------------

window.CHAT_CONFIG = {
  SUPABASE_URL: 'https://rmjqnwzmplihxathfqba.supabase.co',
  SUPABASE_ANON_KEY: 'sb_publishable_dC8rFwPFm5KJGAFQJ9VLHQ_-SziqiFq',
};
